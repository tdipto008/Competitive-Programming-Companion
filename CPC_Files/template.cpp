#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include "C:/CPC_Files/debug.h"
#else
#define dbg(...)
#endif

#define endl "\n"
#define ll long long

void solve() {
    
}

signed main () {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int TT = 1;
    cin >> TT;

    while (TT--) {
        solve();
    }
    return 0;
}
